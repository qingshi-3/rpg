# Acceptance

Accepted on 2026-06-03.

The user approved the layered runtime direction:

```text
perception facts stay outside the state machine
battle-group commander state controls plan and local combat
actor state machines only execute movement and attack phases
module boundaries must prevent tightly coupled changes
```

Implementation must proceed through a focused implementation proposal after the expected authority documents are merged and this design proposal is archived.
