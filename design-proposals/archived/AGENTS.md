# Archived Proposal Rules

This directory is archive-only.

- Do not read archived proposal folders by default.
- Use `README.md` summaries for orientation.
- Only read a specific archived proposal when the user explicitly asks for it.
- Archived proposal content must not override current authority documents in `gameplay-design/` or `system-design/`.
