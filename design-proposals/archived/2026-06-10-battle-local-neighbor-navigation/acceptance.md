# Acceptance

Status: Accepted

The user accepted the expected architecture on 2026-06-10 and requested execution.

Accepted scope:

- Remove Runtime flow-field construction from first-slice battle combat hot paths.
- Keep square-grid topology, footprint legality, occupancy, and reservations as movement authority.
- Treat obstacle avoidance as bounded local neighbor resolution.
- Preserve attack slot, support slot, queue, hold, and pressure semantics as state-machine behavior rather than flow-field behavior.
